{
  "name": "hexo-theme-foundation",
  "repository" :
  { "type" : "git"
  , "url" : "https://github.com/chrisjlee/hexo-theme-zurb-foundation.git"
  },
  "version": "0.1.0",
  "devDependencies": {
    "browser-sync": "^2.7.7",
    "gulp": "^3.8.11",
    "gulp-bower": "0.0.10",
    "gulp-clean": "^0.3.1",
    "gulp-load-plugins": "^0.10.0",
    "gulp-notify": "^2.2.0",
    "gulp-sass": "^1.3.3",
    "node-inspector": "^0.10.1",
    "run-sequence": "^1.1.0"
  },
  "scripts": {
    "setup": "bower install && gulp install",
    "deploy": "cp -rfv ../../public . && git subtree push --prefix dist origin gh-pages"
  }
}
